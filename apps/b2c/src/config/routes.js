export const ROUTES = {
    HOME: '/',
    COLLECTIONS: '/collections',
    COLLECTION: (slug) => `/collections/${slug}`,
    PRODUCT: (slug) => `/product/${slug}`,
    STORY: '/story',
    STORES: '/stores',
    CONTACT: '/contact',
}
