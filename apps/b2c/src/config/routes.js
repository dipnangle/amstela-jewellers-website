export const ROUTES = {
    HOME: '/',
    COLLECTIONS: '/collections',
    COLLECTION: (slug) => `/collections/${slug}`,
    PRODUCT: (slug) => `/product/${slug}`,
    ABOUT: '/about',
    STORES: '/stores',
    CONTACT: '/contact',
}
